// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://stokeus.com',
	// For GitHub Pages deployment, uncomment and update with your username:
	// site: 'https://<username>.github.io/stokeus-website/',
});
